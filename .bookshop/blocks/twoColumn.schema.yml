label: Two Column
name: twoColumn
props:
  side:
    type: select
    label: Side
    options:
      - left
      - right
  blocks:
    type: blocks
    label: Blocks
    _inputs:
      side:
        type: select
        options:
          values:
            - left
            - right 