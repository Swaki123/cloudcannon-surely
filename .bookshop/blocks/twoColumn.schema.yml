label: Two Column
name: twoColumn
props:
  blocks:
    type: blocks
    label: Blocks
    _inputs:
      side:
        type: select
        options:
          values:
            - left
            - right 